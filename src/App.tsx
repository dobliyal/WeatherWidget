import React, { useState } from 'react';
import WeatherWidget from './components/WeatherWidget';

const App: React.FC = () => {
    return (
      <div>
      <WeatherWidget />
  </div>
    );
};

export default App;
